# Même fichier que backend/.env — pour lancer uvicorn hors Docker (Postgres sur localhost:5433).

DATABASE_URL=postgresql+psycopg2://audio:audio@localhost:5433/audio_db
SECRET_KEY=dev-secret-change-me-in-production
PORT=8000

ADMIN_EMAIL=admin@local.dev
ADMIN_PASSWORD=admin123

# FRONTEND_STATIC_DIR laissé vide → backend/app/main.py pointe vers ../frontend
AUDIO_STORAGE_DIR=./data/audio

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WEBHOOK_TOKEN=dev-twilio-token

OPENAI_API_KEY=
OPENAI_AUDIO_MODEL=gpt-audio-mini
OPENAI_JSON_FIX_MODEL=gpt-4o-mini

REACTIVATION_WEBHOOK_TOKEN=dev-reactivation-token
TEXTMEBOT_ENABLED=0
